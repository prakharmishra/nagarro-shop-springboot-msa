---
server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/prakharmishra/nagarro-shop-springboot-msa-config
  application:
    name: nagarro-shop-config-server
  profiles:
    active:
    - dev